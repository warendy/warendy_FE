import { atom } from "recoil";

export const acidityState = atom({
  key: "acidityState",
  default: 0,
});

export const tanninState = atom({
  key: "tanninState",
  default: 0,
});

export const dryState = atom({
  key: "dryState",
  default: 0,
});

export const bodyState = atom({
  key: "bodyState",
  default: 0,
});
